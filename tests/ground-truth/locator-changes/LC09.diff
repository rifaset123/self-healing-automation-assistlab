LC09 — Detail lowongan asistensi
═══════════════════════════════

INFORMASI
- adalah Baseline
+ adalah Ground Truth

File: locators/assistantLocator.ts
─────────────────────────────────
-  vacancyCourseName: (page: Page, courseClassCode: string) =>
-    page
-      .locator(".card-course")
-      .filter({ has: page.getByText(courseClassCode) })
-      .getByTestId("see-details-btn"),

+  vacancyCourseName: (page: Page, courseClassCode: string) =>
+    page
+      .locator(".card-course")
+      .filter({ has: page.getByText(courseClassCode) })
+      .getByTestId("see-details-vacancy-btn"),

Perubahan pada SUT
═══════════════════════════════

INFORMASI SUT
- adalah Baseline
+ adalah Kode dengan injeksi gangguan

File: AssistLab-Frontend-main/resources/views/student/dashboard/assistance_vacancies.blade.php
─────────────────────────────────
- <a href="{{ route('student.courseDetails', ['id' => $item['id']]) }}" data-testid="see-details-btn" ...>

+ <a href="{{ route('student.courseDetails', ['id' => $item['id']]) }}" data-testid="see-details-vacancy-btn" ...>
