The Playwright test script in this directory fails to execute after
changes were made to the web application. Repair the script so that
it runs successfully and correctly tests the intended feature.

Context:
- Test scripts are located in the tests/ directory and follow the
  Page Object Model structure, separated into locators/, pages/,
  and tests/.
- The System Under Test (SUT) is a running web application that can
  be accessed.
- Failure logs are available in the terminal output.

Tasks:
1. Diagnose the cause of the script failure.
2. Apply necessary repairs to the relevant files, using a stable
   locator strategy that does not depend on frequently changing
   visual elements or textual content.
3. Ensure the repair does not alter the original intent of the test.
4. Re-run the script to verify that the repair is successful.